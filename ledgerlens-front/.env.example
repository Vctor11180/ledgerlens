# URL base del backend LedgerLens (sin barra final).
# En desarrollo, si no defines nada, Vite usa http://localhost:3001
# En producción (Vercel mismo dominio) suele dejarse vacío para usar el mismo origen.
# VITE_API_URL=http://localhost:3001

# Red de cobro x402 (debe coincidir con X402_NETWORK del backend): fuji | mainnet
# VITE_X402_NETWORK=fuji
